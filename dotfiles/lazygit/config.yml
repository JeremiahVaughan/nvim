os:
  editPreset: 'nvim'
  editCommand: 'floaterm' # When editing files open up the file in the same instance of nvim
gui:
  theme:
    selectedLineBgColor:
      - underline
    selectedRangeBgColor:
      - underline
git:
  pagers:
    - pager: delta --dark --paging=never
