_, err := config.DB.Exec(
    "INSERT INTO stress_test_request (build_no, service_name) VALUES ($1, $2)",
    r.BuildNumber,
    r.ServiceName,
)
if err != nil {
    return fmt.Errorf("error, when attempting to persist a request for a stress test: %v", err)
}

